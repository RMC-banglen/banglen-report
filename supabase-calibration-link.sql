-- ============================================================
-- เชื่อมรายการรอบงานประจำกับแหล่งข้อมูลอื่น
-- ค่าที่รองรับ: '' (กรอกเอง) | 'sand_fm' (ผลทดสอบขนาดคละทราย)
-- ============================================================
alter table calibration_items
  add column if not exists link_source text default '';
